# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Config & validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Structured logging
structlog==24.4.0

# Database (wired up starting Phase 4 — pinned now so the lockfile is stable)
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Background jobs (wired up starting Phase 10)
celery==5.4.0
redis==5.2.1
